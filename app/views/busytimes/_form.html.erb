<%= form_with(model: busytime) do |form| %>
  <% if busytime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(busytime.errors.count, "error") %> prohibited this busytime from being saved:</h2>

      <ul>
        <% busytime.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
